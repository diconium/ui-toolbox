import { useState } from 'react';
import { Canvas, Meta, Story } from '@storybook/addon-docs';
import Tag from './Tag';

<Meta
  title="Toolbox/Tag"
  component={Tag}
/>

# Tag

To import the `Tag` component simply include the following at the top of your file:

```
import { Tag } from '@dicoauto/toolbox';
```

<Canvas>
  <Story name="Default">
    <div className="flex space-x-2">
      <Tag label="ECU-2" />
      <Tag label="Audi" />
      <Tag label="Volkswagen" />
    </div>
  </Story>
</Canvas>

<Canvas>
  <Story name="With Action">
    <Tag
      label="Audi"
      onCancel={() => {}}
    />
  </Story>
</Canvas>
