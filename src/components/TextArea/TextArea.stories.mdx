import { useState } from 'react';
import { Canvas, Meta, Story } from '@storybook/addon-docs';
import TextArea from './TextArea';
import Icon from '../Icon';

<Meta
  title="Toolbox/TextArea"
  component={TextArea}
/>

# TextArea

To import the `TextArea` component simply include the following at the top of your file:

```
import { TextArea } from '@dicoauto/toolbox';
```

## Default

<Canvas>
  <Story name="Default">
    <div className="flex flex-col space-y-4">
      <TextArea placeholder="Placeholder text here" />
      <TextArea
        placeholder="Placeholder text here"
        value="With text"
      />
    </div>
  </Story>
</Canvas>

## Label

<Canvas>
  <Story name="Label">
    <div className="flex flex-col space-y-4">
      <TextArea
        label="With label"
        placeholder="Placeholder text here"
      />
      <TextArea
        label="With label"
        placeholder="Placeholder text here"
        value="With text"
      />
    </div>
  </Story>
</Canvas>

## Hint

<Canvas>
  <Story name="Hint">
    <div className="flex flex-col space-y-4">
      <TextArea
        label="With label"
        hint="Your hint"
        placeholder="Placeholder text here"
      />
      <TextArea
        label="With label"
        hint="Your hint"
        placeholder="Placeholder text here"
        value="With text"
      />
    </div>
  </Story>
</Canvas>

## Different rows

The `TextArea`component defaults to 5 rows, if not set explicitly.

<Canvas>
  <Story name="Different rows">
    <TextArea
      rows={10}
      placeholder="Placeholder text here"
      value="With text"
    />
  </Story>
</Canvas>

## State:Focused

<Canvas>
  <Story
    name="Focused"
    parameters={{ pseudo: { focus: true } }}
  >
    <div className="flex flex-col space-y-4">
      <TextArea
        label="With label"
        disabled
        placeholder="Placeholder text here"
      />
      <TextArea
        label="With label"
        disabled
        placeholder="Placeholder text here"
        value="With text"
      />
    </div>
  </Story>
</Canvas>

## With Action

<Canvas>
  <Story name="With Action">
    {() => {
      const [value, set] = useState();
      return (
        <TextArea
          placeholder="Placeholder text"
          value={value}
          onChange={(text) => set(text)}
        />
      );
    }}
  </Story>
</Canvas>

## Disabled

<Canvas>
  <Story name="Disabled">
    <div className="flex flex-col space-y-4">
      <TextArea
        disabled
        placeholder="Placeholder text here"
      />
      <TextArea
        disabled
        placeholder="Placeholder text here"
        value="With text"
      />
    </div>
  </Story>
</Canvas>
