import { Canvas, Meta, Story } from '@storybook/addon-docs';
import IconButton from './Icon';

<Meta title="Toolbox/Button/Icon" component={IconButton} />

# IconButton

To import the `IconButton` component simply include the following at the top of your file:

```
import { IconButton } from '@dicoauto/toolbox';
```

<Canvas>
  <Story name="Default">
    <IconButton icon="check" />
  </Story>
</Canvas>

<Canvas>
  <Story name="With Action">
    <IconButton icon="alarm-clock-1" onClick={() => {}} />
  </Story>
</Canvas>

<Canvas>
  <Story name="Secondary">
    <IconButton secondary icon="bell" onClick={() => {}} />
  </Story>
</Canvas>

<Canvas>
  <Story name="Disabled">
    <IconButton disabled icon="server" onClick={() => {}} />
  </Story>
</Canvas>

<Canvas>
  <Story name="Multiple in a row">
    <div className="flex space-x-1">
      <IconButton icon="check" onClick={() => {}} />
      <IconButton secondary icon="bell" onClick={() => {}} />
      <IconButton icon="book" onClick={() => {}} />
    </div>
  </Story>
</Canvas>
