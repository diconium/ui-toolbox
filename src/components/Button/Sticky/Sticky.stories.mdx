import { Canvas, Meta, Story } from '@storybook/addon-docs';
import StickyButton from './Sticky';

<Meta
  title="Toolbox/Button/Sticky"
  component={StickyButton}
/>

# StickyButton

To import the `StickyButton` component simply include the following at the top of your file:

```
import { StickyButton } from '@dicoauto/toolbox';
```

<Canvas>
  <Story name="Default">
    <div className="h-96 relative">
      <StickyButton label="Action" />
    </div>
  </Story>
</Canvas>

<Canvas>
  <Story name="Left side">
    <div className="h-96 relative">
      <StickyButton
        label="Action"
        left
      />
    </div>
  </Story>
</Canvas>

<Canvas>
  <Story name="Secondary">
    <div className="h-96 relative">
      <StickyButton
        secondary
        label="Action"
      />
    </div>
  </Story>
</Canvas>

<Canvas>
  <Story name="Disabled">
    <div className="h-96 relative">
      <StickyButton
        disabled
        label="Action"
      />
    </div>
  </Story>
</Canvas>
