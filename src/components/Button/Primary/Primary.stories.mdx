import { Canvas, Meta, Story } from '@storybook/addon-docs';
import PrimaryButton from './Primary';

<Meta
  title="Toolbox/Button/Primary"
  component={PrimaryButton}
/>

# PrimaryButton

To import the `PrimaryButton` component simply include the following at the top of your file:

```
import { PrimaryButton } from '@dicoauto/toolbox';
```

<Canvas>
  <Story name="Default">
    <div className="flex">
      <PrimaryButton
        label="Action"
        onClick={() => {}}
      />
      <PrimaryButton
        outlined
        label="Action"
        onClick={() => {}}
      />
      <PrimaryButton
        icon="check"
        label="Action"
        onClick={() => {}}
      />
      <PrimaryButton
        icon="bolt"
        outlined
        label="Action"
        onClick={() => {}}
      />
    </div>
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Hovered"
    parameters={{ pseudo: { hover: true } }}
  >
    <div className="flex">
      <PrimaryButton label="Action" />
      <PrimaryButton
        outlined
        label="Action"
      />
      <PrimaryButton
        label="Action"
        icon="check"
      />
      <PrimaryButton
        outlined
        label="Action"
        icon="bolt"
      />
    </div>
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Active"
    parameters={{ pseudo: { active: true } }}
  >
    <div className="flex">
      <PrimaryButton label="Action" />
      <PrimaryButton
        outlined
        label="Action"
      />
      <PrimaryButton
        label="Action"
        icon="check"
      />
      <PrimaryButton
        outlined
        label="Action"
        icon="bolt"
      />
    </div>
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Focus"
    parameters={{ pseudo: { focusVisible: true } }}
  >
    <div className="flex">
      <PrimaryButton label="Action" />
      <PrimaryButton
        outlined
        label="Action"
      />
      <PrimaryButton
        label="Action"
        icon="check"
      />
      <PrimaryButton
        outlined
        label="Action"
        icon="bolt"
      />
    </div>
  </Story>
</Canvas>

<Canvas>
  <Story name="Disabled">
    <div className="flex">
      <PrimaryButton
        disabled
        label="Action"
      />
      <PrimaryButton
        disabled
        outlined
        label="Action"
      />
      <PrimaryButton
        disabled
        label="Action"
        icon="check"
      />
      <PrimaryButton
        disabled
        outlined
        label="Action"
        icon="bolt"
      />
    </div>
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Visited"
    parameters={{ pseudo: { visited: true } }}
  >
    <div className="flex">
      <PrimaryButton label="Action" />
      <PrimaryButton
        outlined
        label="Action"
      />
      <PrimaryButton
        label="Action"
        icon="check"
      />
      <PrimaryButton
        outlined
        label="Action"
        icon="bolt"
      />
    </div>
  </Story>
</Canvas>
