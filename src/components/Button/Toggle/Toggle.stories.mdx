import { useState } from 'react';
import { Canvas, Meta, Story } from '@storybook/addon-docs';
import ToggleButton from './Toggle';

<Meta
  title="Toolbox/Button/Toggle"
  component={ToggleButton}
/>

# ToggleButton

To import the `ToggleButton` component simply include the following at the top of your file:

```
import { ToggleButton } from '@dicoauto/toolbox';
```

<Canvas>
  <Story name="Default">
    <ToggleButton />
  </Story>
</Canvas>

<Canvas>
  <Story name="With Title">
    <ToggleButton title="Some action" />
  </Story>
</Canvas>

<Canvas>
  <Story name="Toggled">
    <ToggleButton toggled />
  </Story>
</Canvas>

<Canvas>
  <Story name="Disabled">
    <div className="flex space-x-4">
      <ToggleButton
        toggled
        disabled
      />
      <ToggleButton disabled />
    </div>
  </Story>
</Canvas>

<Canvas>
  <Story name="With Action">
    {() => {
      const [toggle, set] = useState(false);
      return (
        <ToggleButton
          toggled={toggle}
          onClick={(newState) => set(newState)}
        />
      );
    }}
  </Story>
</Canvas>
