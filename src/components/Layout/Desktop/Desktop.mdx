import { Canvas, Meta } from '@storybook/blocks';
import * as DesktopStories from './Desktop.stories';

<Meta of={DesktopStories} />

# DesktopLayout

To import the `DesktopLayout` component simply include the following at the top of your file:

```
import { DesktopLayout } from '@diconium/ui-toolbox';
```

## Default

<Canvas of={DesktopStories.Default} />

## Additional styles

<Canvas of={DesktopStories.AdditionalStyles} />
