import { Canvas, Meta, Story } from '@storybook/addon-docs';
import Layout from './Desktop';
import TopNavigation from '../../TopNavigation';
import NavigationBar from '../../NavigationBar';
import Icon from '../../Icon';
import Avatar from '../../Avatar';
import QuickButton from '../../Button/Quick';

<Meta
  title="Toolbox/Layout/Desktop"
  component={Layout}
  parameters={{ viewport: { defaultViewport: 'lg' }, layout: 'fullscreen' }}
  decorators={[(Story) => <Story />]}
/>

# DesktopLayout

To import the `DesktopLayout` component simply include the following at the top of your file:

```
import { DesktopLayout } from '@dicoauto/toolbox';
```

## Default

<Canvas>
  <Story name="Default">
    <Layout
      menu={<div className="bg-toolbox-primary">menu</div>}
      action={<div className="bg-toolbox-secondary w-full">action</div>}
    >
      <div className="p-16 flex flex-col items-center bg-toolbox-neutral-50">MAIN CONTENT</div>
    </Layout>
  </Story>
</Canvas>

## Additional styles

You can apply additional styles to the content area of the layout via the `className` property.

<Canvas>
  <Story name="Additional styles">
    <Layout
      className="bg-toolbox-feedback-orange"
      menu={<div className="bg-toolbox-primary w-full">menu</div>}
      action={<div className="bg-toolbox-secondary w-full">action</div>}
    >
      <div className="p-16 flex flex-col items-center bg-toolbox-neutral-50">MAIN CONTENT</div>
    </Layout>
  </Story>
</Canvas>

## Example

<Canvas>
  <Story name="Example">
    <Layout
      className="bg-toolbox-neutral-50"
      menu={
        <TopNavigation
          title="your app"
          leading="simplified screen building"
          subtitle="with reusable layouts"
        >
          <Icon icon="menu" />
        </TopNavigation>
      }
      action={
        <NavigationBar
          left={
            <>
              <Icon icon="mail-check" />
              <Icon icon="calendar" />
            </>
          }
          right={
            <>
              <Icon icon="fingerprint" />
              <Avatar link="https://i.pravatar.cc/150?u=08188" alt="Mike" badge={4} />
            </>
          }
        >
          <QuickButton icon="check" />
        </NavigationBar>
      }
    >
      <div>MAIN CONTENT</div>
    </Layout>
  </Story>
</Canvas>
