import { Canvas, Meta } from '@storybook/blocks';
import * as MobileStories from './Mobile.stories';

<Meta of={MobileStories} />

# MobileLayout

To import the `MobileLayout` component simply include the following at the top of your file:

```
import { MobileLayout } from '@diconium/ui-toolbox';
```

## Default

<Canvas of={MobileStories.Default} />

## Additional styles

You can apply additional styles to the content area of the layout via the `className` property.

<Canvas of={MobileStories.AdditionalStyles} />

## Example

<Canvas of={MobileStories.Example} />
