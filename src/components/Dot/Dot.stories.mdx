import { Canvas, Meta, Story } from '@storybook/addon-docs';
import Dot from './Dot';

<Meta
  title="Toolbox/Dot"
  component={Dot}
/>

# Dot

To import the `Dot` component simply include the following at the top of your file:

```
import { Dot } from '@dicoauto/toolbox';
```

## Default

<Canvas>
  <Story name="Default">
    <Dot />
  </Story>
</Canvas>

## Different sizes

The Dot component comes in different sizes: `xl, lg, m, s`

<Canvas>
  <Story name="Different sizes">
    <div className="flex space-x-4 items-end">
      <Dot size="xl" />
      <Dot size="lg" />
      <Dot size="m" />
      <Dot size="s" />
    </div>
  </Story>
</Canvas>

## Custom color

<Canvas>
  <Story name="Different colors">
    <div className="flex space-x-4 items-end">
      <Dot size="xl" />
      <Dot size="lg" />
      <Dot size="m" />
      <Dot size="s" />
    </div>
    <div className="flex space-x-4 items-end mt-4">
      <Dot
        color="bg-toolbox-secondary"
        size="xl"
      />
      <Dot
        color="bg-toolbox-secondary"
        size="lg"
      />
      <Dot
        color="bg-toolbox-secondary"
        size="m"
      />
      <Dot
        color="bg-toolbox-secondary"
        size="s"
      />
    </div>
    <div className="flex space-x-4 items-end mt-4">
      <Dot
        color="bg-toolbox-feedback-orange"
        size="xl"
      />
      <Dot
        color="bg-toolbox-feedback-orange"
        size="lg"
      />
      <Dot
        color="bg-toolbox-feedback-orange"
        size="m"
      />
      <Dot
        color="bg-toolbox-feedback-orange"
        size="s"
      />
    </div>
  </Story>
</Canvas>
