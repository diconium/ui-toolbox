import { Canvas, Meta } from '@storybook/blocks';
import * as TooltipStories from './Tooltip.stories';

<Meta of={TooltipStories} />

# Tooltip

To import the `Tooltip` component simply include the following at the top of your file:

```
import { Tooltip } from '@diconium/ui-toolbox';
```

The section below shows how to use the `Tooltip` in your project.

## In Action

<Canvas of={TooltipStories.InAction} />

## Default

<Canvas of={TooltipStories.Default} />

## Different sides

<Canvas of={TooltipStories.DifferentSides} />

## Different variants

<Canvas of={TooltipStories.DifferentVariants} />

## Custom content

<Canvas of={TooltipStories.CustomContent} />
