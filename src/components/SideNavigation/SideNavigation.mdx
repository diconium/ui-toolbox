import { Canvas, Meta } from '@storybook/blocks';
import * as SideNavigationStories from './SideNavigation.stories';

<Meta of={SideNavigationStories} />

# SideNavigation

To import the `SideNavigation` component simply include the following at the top of your file:

```
import { SideNavigation } from '@diconium/ui-toolbox';
```

## Default

<Canvas of={SideNavigationStories.Default} />

## Closed

<Canvas of={SideNavigationStories.Closed} />
