import { Canvas, Meta } from '@storybook/blocks';
import * as TestProviderStories from './TestProvider.stories';

<Meta of={TestProviderStories} />

# TestProvider

To import the `TestProvider` component simply include the following at the top of your file:

```
import { TestProvider } from '@diconium/ui-toolbox';
```

## Default

<Canvas of={TestProviderStories.Default} />

## ComplexComponent

<Canvas of={TestProviderStories.ComplexComponent} />
