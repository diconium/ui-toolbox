import { Canvas, Meta, Story } from '@storybook/addon-docs';
import State from './State';
import StateContainer from './StateContainer';

<Meta
  title="Toolbox/State"
  component={State}
/>

# State

To import the `State` component simply include the following at the top of your file:

```
import { State } from '@dicoauto/toolbox';
```

## Default

<Canvas>
  <Story name="Default">
    <div className="flex space-x-4">
      <State color="bg-toolbox-feedback-green" />
      <State color="bg-toolbox-feedback-orange" />
      <State color="bg-toolbox-feedback-red" />
    </div>
  </Story>
</Canvas>

## Different sizes

<Canvas>
  <Story name="Different sizes">
    <div className="flex space-x-4">
      <State
        size="xl"
        color="bg-toolbox-feedback-green"
      />
      <State
        size="lg"
        color="bg-toolbox-feedback-orange"
      />
    </div>
  </Story>
</Canvas>

## With label

<Canvas>
  <Story name="With label">
    <div className="flex flex-col space-y-4">
      <State
        label="Online"
        color="bg-toolbox-feedback-green"
      />
      <State
        label="Not at desk"
        color="bg-toolbox-feedback-orange"
      />
      <State
        label="Offline"
        color="bg-toolbox-feedback-red"
      />
    </div>
  </Story>
</Canvas>

## Attach state to any other component

<Canvas>
  <Story name="Attach state to any other component">
    <StateContainer
      color="bg-toolbox-feedback-orange"
      size="xl"
    >
      <div className="w-16 h-16 bg-toolbox-primary" />
    </StateContainer>
  </Story>
</Canvas>
