import { useState } from 'react';
import { Canvas, Meta, Story } from '@storybook/addon-docs';
import Checkbox from './Checkbox';

<Meta
  title="Toolbox/Checkbox"
  component={Checkbox}
/>

# Checkbox

To import the `Checkbox` component simply include the following at the top of your file:

```
import { Checkbox } from '@dicoauto/toolbox';
```

<Canvas>
  <Story name="Default">
    <Checkbox />
  </Story>
</Canvas>

<Canvas>
  <Story name="Checked">
    <Checkbox checked />
  </Story>
</Canvas>

<Canvas>
  <Story name="Disabled">
    <div className="flex space-x-4">
      <Checkbox disabled />
      <Checkbox
        disabled
        checked
      />
    </div>
  </Story>
</Canvas>

<Canvas>
  <Story name="With Action">
    {() => {
      const [checked, set] = useState(false);
      return (
        <Checkbox
          checked={checked}
          onClick={() => set(!checked)}
        />
      );
    }}
  </Story>
</Canvas>
