import { Canvas, Meta, Story } from '@storybook/blocks';
import Form from './Form';
import * as FormStories from './Form.stories';

<Meta of={FormStories} />

# Form

To import the `Form` component simply include the following at the top of your file:

```
import { Form } from '@diconium/ui-toolbox';
```

## Default

<Canvas of={FormStories.Default} />

## With Input

<Canvas of={FormStories.WithInput} />
